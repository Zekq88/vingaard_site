apiVersion: v1
kind: Service
metadata:
  name: vingaard-frontend-service
spec:
  selector:
    app: vingaard-frontend
  ports:
    - protocol: TCP
      port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}