apiVersion: v1 
kind: Service
metadata:
  name: vingaard-backend-service
spec:
  selector:
    app: vingaard-backend
  ports:
    - protocol: TCP
      port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}